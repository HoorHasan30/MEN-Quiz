# Node & Express Review Quiz

1. What stack are we doing for this Unit<br>
A: MEN

2. Node is a programming language (True or False)<br>
A: False

3. What is the protocol that the computers are using to send the requests and responses?<br>
A: HTTPS

4. When we send a request from the client to the server what always happens next?<br>
A: The server sends a response

5. List at least 2 HTTP methods below<br>
A: GET, POST, PUT, DELETE


6. In your words describe to me what the server is and what the client is<br>
A: 
- A Clinet is what the user use to communicate with the server through by sending requests 
- A Server communicate with the client by sending responses back to the user through the client

7. What is the difference between error messages that start with 400 vs ones with 500? <br>
A: 400 is client issues, while 500 is server issues

8. What is the first step you always run to initialize node projects <br>
A: npm init -y

9. true or false? the code for packages we download is stored in the package.json? <br>
A: False

10. You cloned code from github and you see that there isn’t the node_modules folder but there is a package. json file with the dependencies. What command do you run to download all the dependencies? <br>
A: npm i 